---
phase: 02-dashboard-cardiac-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/20260314_add_cardiac_metric/migration.sql
  - backend/src/lib/riskScores.ts
  - backend/src/lib/riskScores.test.ts
autonomous: true
requirements:
  - CARD-01
  - CARD-02
must_haves:
  truths:
    - "CardiacMetric table exists in the DB schema with all biomarker columns"
    - "computeGrace() and computeCha2ds2vasc() return correct integer scores for known inputs"
    - "computeGrace() returns null when minimum inputs (age) are absent"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "CardiacMetric model definition"
      contains: "model CardiacMetric"
    - path: "backend/prisma/migrations/20260314_add_cardiac_metric/migration.sql"
      provides: "DDL for cardiac_metrics table"
      contains: "CREATE TABLE cardiac_metrics"
    - path: "backend/src/lib/riskScores.ts"
      provides: "Pure server-side risk score functions"
      exports: ["computeGrace", "computeCha2ds2vasc"]
    - path: "backend/src/lib/riskScores.test.ts"
      provides: "Unit tests for risk score functions"
  key_links:
    - from: "backend/src/lib/riskScores.ts"
      to: "backend/src/routes/patients.ts"
      via: "named import"
      pattern: "import.*computeGrace.*riskScores"
---

<objective>
Add the CardiacMetric Prisma model and implement server-side risk score computation functions.

Purpose: These are the foundational contracts all other Phase 2 plans build on. The migration creates the table; the pure functions provide the scoring logic consumed by the patients API in Plan 02.
Output: schema.prisma with CardiacMetric, manual migration SQL, riskScores.ts with tested pure functions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-cardiac-metrics/02-RESEARCH.md
@backend/prisma/schema.prisma
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CardiacMetric model to Prisma schema + manual migration SQL</name>
  <files>backend/prisma/schema.prisma, backend/prisma/migrations/20260314_add_cardiac_metric/migration.sql</files>
  <behavior>
    - CardiacMetric model exists in schema.prisma with fields: id, patientId, recordedAt, recordedById, ejectionFraction (Decimal 5,2), nyhaClass (Int), ntProBnp (Decimal 10,2), bnp (Decimal 10,2), hsTroponinI (Decimal 10,4), hsTroponinT (Decimal 10,4), creatinine (Decimal 8,2), killipClass (Int), notes (String), createdAt
    - Patient model has `cardiacMetrics CardiacMetric[]` relation added
    - User model has `cardiacMetricsRecorded CardiacMetric[]` relation added (recordedBy)
    - migration.sql creates cardiac_metrics table with all columns and FK constraints
    - Index on (patient_id, recorded_at) exists in migration SQL
  </behavior>
  <action>
    1. Add CardiacMetric model to backend/prisma/schema.prisma exactly as specified in RESEARCH.md Pattern 3. Map table to "cardiac_metrics".
    2. Add relation `cardiacMetrics CardiacMetric[]` to the Patient model.
    3. Add relation `cardiacMetricsRecorded CardiacMetric[] @relation("CardiacMetricRecorder")` to the User model and update the CardiacMetric `recordedBy` relation name to match.
    4. Create directory backend/prisma/migrations/20260314_add_cardiac_metric/ and write migration.sql manually (no prisma migrate dev — no live DB per STATE.md decision).
    Migration SQL must include:
    - CREATE TABLE cardiac_metrics with all columns matching the Prisma model
    - FK: REFERENCES patients(id) ON DELETE CASCADE
    - FK: REFERENCES users(id) ON DELETE SET NULL (for recorded_by)
    - CREATE INDEX ON cardiac_metrics(patient_id, recorded_at)
  </action>
  <verify>
    <automated>cd /home/recep/Desktop/Machine_Learning/projects/health_monitor_gp/signal-guide-health/backend && npx prisma validate 2>&1</automated>
  </verify>
  <done>prisma validate exits 0; schema.prisma contains "model CardiacMetric"; migration.sql exists with CREATE TABLE cardiac_metrics</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and test server-side risk score pure functions</name>
  <files>backend/src/lib/riskScores.ts, backend/src/lib/riskScores.test.ts</files>
  <behavior>
    - computeCha2ds2vasc({dateOfBirth, gender, chronicConditions}) returns integer 0-9
    - age >= 75 adds 2 points; age 65-74 adds 1 point; female gender adds 1 point
    - chronicConditions matching /heart failure/i adds 1; /hypertension/i adds 1; /stroke|tia/i adds 2; /vascular disease|peripheral arterial|mi/i adds 1; /diabetes/i adds 1
    - computeGrace({age, heartRate?, systolicBP?, creatinine?}) returns integer 0-360 or null
    - Returns null when age is not provided or NaN
    - Uses published GRACE 2.0 age lookup: <30→0, 30-39→8, 40-49→25, 50-59→41, 60-69→58, 70-79→75, >=80→91
    - HR points: <50→0, 50-69→3, 70-89→9, 90-109→15, 110-149→24, 150-199→38, >=200→46
    - SBP points: <80→58, 80-99→53, 100-119→43, 120-139→34, 140-159→24, 160-199→10, >=200→0
    - Test: 65yr male, HR=85, SBP=130 → GRACE = 58+9+34 = 101
    - Test: no age → returns null
    - Test: 70yr female with heart failure, hypertension → CHA2DS2-VASc = 1(age)+1(female)+1(HF)+1(HTN) = 4
  </behavior>
  <action>
    Create backend/src/lib/riskScores.ts with:
    - Import differenceInYears from date-fns (already in project)
    - Export computeCha2ds2vasc(patient: {dateOfBirth: Date; gender: string | null; chronicConditions: string[];}): number
    - Export computeGrace(inputs: {age: number; heartRate?: number | null; systolicBP?: number | null; creatinine?: number | null;}): number | null
    - computeGrace returns null if inputs.age is not a finite number
    - Use the GRACE 2.0 lookup tables from RESEARCH.md (age, HR, SBP buckets)
    - Creatinine points if provided: <0.39→1, 0.4-0.79→4, 0.8-1.19→7, 1.2-1.59→10, 1.6-1.99→13, 2.0-3.99→21, >=4→28

    Write backend/src/lib/riskScores.test.ts with Vitest tests covering:
    - GRACE null on missing age
    - GRACE correct score for known inputs (65yr, HR=85, SBP=130 → 101)
    - CHA2DS2-VASc for female 70yr with HF+HTN → 4
    - CHA2DS2-VASc for male 50yr no conditions → 0
  </action>
  <verify>
    <automated>cd /home/recep/Desktop/Machine_Learning/projects/health_monitor_gp/signal-guide-health && npx vitest run backend/src/lib/riskScores.test.ts 2>&1</automated>
  </verify>
  <done>All riskScores tests pass; computeGrace and computeCha2ds2vasc exported from riskScores.ts</done>
</task>

</tasks>

<verification>
- `npx prisma validate` exits 0
- `npx vitest run backend/src/lib/riskScores.test.ts` all tests green
- migration.sql file exists at backend/prisma/migrations/20260314_add_cardiac_metric/migration.sql
- `npx tsc --noEmit` in backend still exits 0
</verification>

<success_criteria>
CardiacMetric Prisma model validated; risk score pure functions tested with known clinical values; no TypeScript errors introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-cardiac-metrics/02-01-SUMMARY.md`
</output>
