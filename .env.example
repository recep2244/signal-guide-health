# =============================================================================
# CardioWatch Environment Configuration
# Copy this file to .env and fill in your values
# =============================================================================

# API Configuration
VITE_API_BASE_URL=https://api.cardiowatch.com/api/v1
VITE_WS_URL=wss://api.cardiowatch.com/ws

# WhatsApp/Wearable credentials must stay server-side only.
# Configure those in backend env/admin keys, never in Vite client env.

# Authentication
VITE_AUTH_DOMAIN=auth.cardiowatch.com
VITE_AUTH_CLIENT_ID=your_auth_client_id
VITE_AUTH_AUDIENCE=https://api.cardiowatch.com

# Push Notifications (Firebase Cloud Messaging)
VITE_FCM_VAPID_KEY=your_fcm_vapid_key
VITE_FCM_PROJECT_ID=your_firebase_project_id

# Analytics (Optional)
VITE_ANALYTICS_ID=your_analytics_id
VITE_SENTRY_DSN=your_sentry_dsn

# Feature Flags
VITE_ENABLE_MOCK_DATA=true
VITE_ENABLE_PILOT_MOCK_DATA=false
VITE_ENABLE_REAL_TIME_SYNC=false
VITE_ENABLE_PUSH_NOTIFICATIONS=false
VITE_ENABLE_ADMIN_UI=true
VITE_DEPLOY_TARGET=cloudflare
# Optional explicit base path override (example: /signal-guide-health/)
# VITE_BASE_PATH=/

# Environment
VITE_APP_ENV=development
